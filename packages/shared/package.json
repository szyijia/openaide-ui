{
  "name": "@openaide/shared",
  "version": "0.1.0",
  "private": true,
  "description": "OpenAIDE — 共享类型和协议定义",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@openaide/protocol": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}